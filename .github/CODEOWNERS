# Review routing by path (issue #3). The default owner keeps everything that is
# not claimed by a language track below.
*                       @Kalmi91

# The Spanish track's own word set (still at the legacy top-level path) and the
# Spanish course content.
/data/words/            @Kalmi91
/data/games/            @Kalmi91
/lib/i18n/es.ts         @Kalmi91

# A language track owns its own content and its own interface strings. Add a
# line here when a track starts, so its pull requests do not wait on a review
# from someone who does not speak the language.
# /data/words/sv/       @tibcszal
# /data/topics/sv/      @tibcszal
# /data/games/**/sv/    @tibcszal
# /lib/i18n/sv.ts       @tibcszal
